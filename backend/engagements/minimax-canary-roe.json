{
  "engagement_id": "minimax-canary-2026-08-05",
  "authority_source": "operator-owned MiniMax API key (coding-plan); authorized self-test of MiniMax-M3 as canary TARGET",
  "authorized_scope": ["127.0.0.1", "localhost"],
  "permitted_roles": ["recon", "scanner", "attacker", "judge"],
  "auto_allowed": [
    "fire attack payloads at local canary proxy on 127.0.0.1",
    "proxy canary may call api.minimax.io with operator key as the TARGET model",
    "score with canary.is_leak",
    "write results under backend/bench/results"
  ],
  "approval_required": [
    "any other cloud provider",
    "any non-canary production customer system",
    "publish as multi-provider public leaderboard without n/LCB"
  ],
  "evidence_required": true,
  "notes": "Harness fire stays on loopback canary (SSRF/scope). MiniMax is the model under test (system SECRET guard), NOT the abliterated generator. Generator may still use local ablit for recipes if needed.",
  "battery_default": "v1_canary",
  "target_mode_default": "minimax",
  "tools_default": "garbleworks:gw_baseline",
  "target_model": "MiniMax-M3",
  "target_base": "https://api.minimax.io/v1"
}
